#setting h2database console dashboard
spring:
  h2:
    console:
      enabled: true
      path: /h2-console
        datasource:    url: jdbc:h2:~/test;
    driverClassName: org.h2.Driver
    username: sa
    password:
spring.jpa.database-platform: org.hibernate.dialect.H2Dialect